package kot

/**
 * Collections
 * -- all collections by default Immutable, if we need mutable collections, so ew need to create
 * MutableList, MutableSet, MutableMap and e.t.c.
 *
 */
fun main() {


}